services:
  handwriting-ai-lab:
    build: .
    ports:
      - "8081:8081"
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
      PORT: "8081"
      HANDWRITING_DATA: /data
    volumes:
      - handwriting-brains:/data

volumes:
  handwriting-brains:
